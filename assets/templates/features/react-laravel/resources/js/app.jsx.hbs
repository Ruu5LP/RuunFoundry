import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return (
    <main>
      <h1>{{projectName}}</h1>
      <p>FoundRuu で初期化された Laravel + React プロジェクトです。</p>
    </main>
  );
}

ReactDOM.createRoot(document.getElementById("app")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
